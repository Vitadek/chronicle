{
  "//": "For editor/IDE support while authoring. Chronicle's SERVER compiles the plugin (esbuild), so you never need to run a build yourself. `@chronicle/plugin-api` is provided by the host at runtime; this path just gives you types.",
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "esModuleInterop": true,
    "paths": {
      "@chronicle/plugin-api": ["../../src/plugins/api/index.ts"]
    }
  },
  "include": ["src/**/*"]
}
